<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Version-specific theme overrides -->
    <style name="Base.Theme.ChordMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/warm_primary</item>
        <item name="colorPrimaryVariant">@color/warm_primaryDark</item>
        <item name="colorOnPrimary">@color/warm_text</item>
        <item name="android:statusBarColor">@color/warm_primaryDark</item>
        <item name="android:navigationBarColor">@color/warm_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>